<!--pages/search/index.wxml-->
<view wx:for="{{songs}}">
  <view class="name" data-id="{{item.id}}" bind:tap="gotoPlayMusic">
    {{item.name}} - {{item.artists[0].name}}
    </view>
    <image src="{{item.artists[0].img1v1Url}}" />
</view>